@model Belgo.Web.Models.PesquisaModel

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title">Excluir pesquisa</h4>
</div>
<div class="modal-body">
    <p>Tem certeza que deseja excluir esta pesquisa?</p>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-default" data-dismiss="modal">Cancelar</button>
    <a href="@Url.Action("Excluir", new {id = Model.ID, efetuar = true })" role="button" class="btn btn-danger">Excluir</a>
</div>